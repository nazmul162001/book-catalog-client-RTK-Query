import { RingLoader } from 'react-spinners';


const Loader = () => {
  return (
    <div className="loader-container">
      <RingLoader color="#379CF0" size={80} />
    </div>
  );
};

export default Loader